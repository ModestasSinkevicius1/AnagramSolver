@{
    ViewData["Title"] = "Dictionary Page";
    
    var words = ViewData["Words"] as List<string>;  
}

@foreach(var word in words)
{
    <h1>@Html.ActionLink(word, "Index", @"Home", new { myWords = word }, null)</h1>    
}

